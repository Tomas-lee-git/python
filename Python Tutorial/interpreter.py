# command: python3.12, show python's information;

# command + D: quit python interpreter;

# sys.argv: the script name and additional arguments thereafter are turned into a list of strings and assigned to the argv variable in the sys module;

# Continuation lines are needed when entering a multi-line construct(Shift + Enter);

# use Enter to execute in command line

# By default, Python source files are treated as encoded in UTF-8;
